Divine Taranga Silks

## AI Saree Try-On

- Set `OPENAI_API_KEY` in `.env`
- On a product page, click `Try On (AI)` to upload a photo and generate results

Note: The try-on endpoint generates portrait images at `1024x1536` (9:16). If you need true 4K output, add an upscaling step after generation.
